{
  "//": "jdsl capture hooks (design §29.1). Each hook forwards Claude Code's structured JSON payload to the local harness ingest daemon (jdsl harness serve). The forwarder fails open — it never blocks or fails a tool call (§7.2). Set JDSL_CAPTURE_ID to route events into a specific capture; JDSL_INGEST_URL overrides the default http://127.0.0.1:8848.",
  "SessionStart": [
    { "hooks": [{ "type": "command", "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/jdsl_hook_forwarder.py" }] }
  ],
  "UserPromptSubmit": [
    { "hooks": [{ "type": "command", "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/jdsl_hook_forwarder.py" }] }
  ],
  "PreToolUse": [
    { "matcher": "*", "hooks": [{ "type": "command", "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/jdsl_hook_forwarder.py" }] }
  ],
  "PostToolUse": [
    { "matcher": "*", "hooks": [{ "type": "command", "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/jdsl_hook_forwarder.py" }] }
  ],
  "SessionEnd": [
    { "hooks": [{ "type": "command", "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/jdsl_hook_forwarder.py" }] }
  ]
}
